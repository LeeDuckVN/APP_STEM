# MongoDB Atlas connection string. Keep the real value in .env and never commit it.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=stem_iot
PORT=3000
